<pre>
$(document).ready(function(){
  // The CSS is responsible for showing sprite
  // The CSS is responsible for setting location
  // No need to set a direction for sprite movement
  var isMoving = true;
  var $sprite = $('.sprite');

</pre>
<pre class="highlight">
  var switchCostume = function(){
</pre>
<pre>
    setTimeout(function(){
      if ($sprite.hasClass('costume-next')){
        $sprite.removeClass('costume-next');
      } else {
        $sprite.addClass('costume-next');
      }
      if (isMoving) {
        switchCostume();
      }
    }, 200);
</pre>
<pre class="highlight">
  };
</pre>
<pre>

  $sprite.animate({
    left: 300
  }, 3 * 1000, 'linear', function(){
    isMoving = false;
  });

  switchCostume();
});
</pre>
