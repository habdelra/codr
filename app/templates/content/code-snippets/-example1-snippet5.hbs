<pre>
$(document).ready(function() {
  // The CSS is responsible for showing sprite
  // The CSS is responsible for setting location
  // No need to set a direction for sprite movement
  var $sprite = $('.sprite');
  var xOffset = $sprite.position().left;
  var yOffset = $sprite.position().top;
  var i;
  for (i = 0; i < 4; i++) {
</pre>
<pre class="highlight">
    xOffset += 50;
</pre>
<pre>
    yOffset -= 50;
    $sprite.animate({
      left: xOffset
    })
    .animate({
      top: yOffset
    });
  }
});
</pre>
